class User {
  final String username;
  final String password;
  final String fullname; // เพิ่ม properties ต่าง ๆ ตามที่ต้องการ

  User({required this.username, required this.password, required this.fullname});
}
